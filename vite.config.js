
import { defineConfig } from 'vite';

export default defineConfig({
    // Vercel Serverless Functions handle API routes (/api/*). 
    // Use `vercel dev` or `npx vercel dev` for local development.
});
